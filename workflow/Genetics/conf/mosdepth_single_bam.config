/*
 * Single-sample mosdepth via mosdepth_single_bam.nf (ct_depth_with_mosdepth).
 * Pass alongside nextflow.config: -c .../conf/mosdepth_single_bam.config
 */
params.mosdepth_cpus = 10

process {
    withName: 'ct_depth_with_mosdepth' {
        cpus = 10
        memory = '4 GB'
    }
}
