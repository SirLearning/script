install.packages("pak")
pak::pak('itol.toolkit')
pwd
pwd()
# load package
library(itol.toolkit)
# read data
tree <- system.file("extdata",
"DTC.tree",
package = "itol.toolkit")
data("template_groups")
df_group <- data.frame(id = unique(template_groups$group),
data = unique(template_groups$group))
# create hub
hub <- create_hub(tree = tree)
install.packages(table2itol)
devtools::install_github("mgoeker/table2itol")
install.packages(devtools)
install.packages("devtools")
library(devtools)
devtools::install("mgoeker/table2itol")
package_list = c("grid","ggplot2","gridExtra","vegan","reshape2","readODS")
for(p in package_list){
if(!suppressWarnings(suppressMessages(require(p, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)))){
install.packages(p, repos=site)
suppressWarnings(suppressMessages(library(p, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)))
}
}
install("vegan")
install.packages("vegan")
for(p in package_list){
if(!suppressWarnings(suppressMessages(require(p, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)))){
install.packages(p, repos=site)
suppressWarnings(suppressMessages(library(p, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)))
}
}
install.packages("reshape2")
for(p in package_list){
if(!suppressWarnings(suppressMessages(require(p, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)))){
install.packages(p, repos=site)
suppressWarnings(suppressMessages(library(p, character.only = TRUE, quietly = TRUE, warn.conflicts = FALSE)))
}
}
install.packages("read0DS")
source("http://bioconductor.org/biocLite.R")
install.packages("optparse")
