# Progress — 2026-07-05

## 2026-07-05 — MAC=0 FastCall3 read-only trace tool

- Added `src/python/genetics/genomics/variant/fastcall3_site_trace.py`: decode `.ing.gz` / `.iac.gz`, index sites via `gen/*_1_*.pos.txt` (no vlib; no writes to FastCall intermediates).
- Cohort scan: all taxa `.iac` (scan ref/alt/missing) + all taxa `.ing` (disc alt discoveries for blib MAO drivers).
- Tests: `tests/test_fastcall3_site_trace.py` — **5 passed**.
- Run folder: `08stats.genome/97run_mac0_fastcall_trace/`; pilot one site (`2-44569502`) ~8 min; full 50-site trace launched in screen `mac0_fc_trace`.

## 2026-07-05 — thin_common_compare sample missing slope plots

- Extended `plot_slope_chart` in `infra/utils/graph.py` with optional `group_col` (uses `sample_group_palette` hues) plus `ylabel` / `ylim`.
- Added `plot_thin_common_sample_missing_slope` in `results_io.py`: per subgenome (A/B/D/Others) slope chart connecting each sample's `F_MISS` from `test_thin` → `test_common_thin`; writes `*.sample.missing_slope.{subgenome}.png` and `*.sample.missing_slope.info.tsv`.
- Wired into `plot_thin_common_chr_variant_compare` (`stats_chr_report.nf`); group labels from `{output_dir}/meta_data/sample_group.txt`.
- Smoke test on `test_plink` data: **4** subgenome PNGs generated under `/tmp/thin_common_miss_slope_test/`.

## 2026-07-05 — SUPP — thin_common_miss_compare refactor

- Moved `plot_thin_common_sample_missing_slope` from `results_io.py` → `miss.py` (sample missing analysis, not PLINK I/O).
- Removed missing slopes from `chr_compare` / `plot_thin_common_chr_variant_compare` (that task is variant-count/density only).
- New partial task `--partial_task thin_common_miss_compare` → `plot_thin_common_sample_missing_compare` in `stats_sample.nf`; publishes to `stats/thin_common_compare/` without per-chromosome variant work.
- Run: `08stats.genome/99run_thin_common_miss_compare/` (screen `thin_miss_cmp`; exit 0, 1 succeeded ~6m).

<!-- nf-replay -->
#### NF replay — thin_common_miss_compare

Working directory: `/data/home/tusr1/01projects/vmap4/08stats.genome/99run_thin_common_miss_compare`. Outcome: exit 0, 1 succeeded.

```bash
source ~/.bashrc && conda activate run && \
screen -dmS thin_miss_cmp bash -lc 'source ~/.bashrc && conda activate run && cd /data/home/tusr1/01projects/vmap4/08stats.genome/99run_thin_common_miss_compare && nextflow run /data/home/tusr1/git/script/workflow/Genetics/subworkflows/local/entry/partial_router.nf -c /data/home/tusr1/git/script/workflow/Genetics/nextflow.config --home_dir /data/home/tusr1/01projects/vmap4 --user_dir /data/home/tusr1 --src_dir /data/home/tusr1/git/script/src --partial_task thin_common_miss_compare --output_dir /data1/dazheng_tusr1/vmap4.VCF.v1 --job test_plink 2>&1 | tee run_logs/thin_common_miss_compare.log'
```
<!-- /nf-replay -->

## 2026-07-05 — test_common_only mod (MAF-only + 2% thin)

- New mod **`test_common_only`**: per-chr `plink2 --maf 0.05 --thin 0.02` (no `--geno`), merge A/B/D/Others; parallel to `test_common_thin` but drops variant missing filter.
- Code: `subsampling_maf_only_pfile_for_test`, `test_common_only_processor`, `RUN_TEST_COMMON_ONLY` in `main.nf`.
- `plot_thin_common_sample_missing_slope` accepts `left_label` / `right_label`; partial compare uses `compare_process_dir` + `left_mod_label` / `right_mod_label`.
- Run: `08stats.genome/100run_test_common_only_build/` (screen `test_common_only`) — full `main.nf` build + stats (ref IBS plots) then `thin_common_miss_compare` vs `test_thin`.

<!-- nf-replay -->
#### NF replay — test_common_only build + miss slope vs test_thin

Working directory: `/data/home/tusr1/01projects/vmap4/08stats.genome/100run_test_common_only_build`.

```bash
source ~/.bashrc && conda activate run && \
screen -dmS test_common_only bash -lc 'source ~/.bashrc && conda activate run && cd /data/home/tusr1/01projects/vmap4/08stats.genome/100run_test_common_only_build && \
nextflow run /data/home/tusr1/git/script/workflow/Genetics/main.nf \
  -c /data/home/tusr1/git/script/workflow/Genetics/nextflow.config \
  --home_dir /data/home/tusr1/01projects/vmap4 --user_dir /data/home/tusr1 --src_dir /data/home/tusr1/git/script/src \
  --output_dir /data1/dazheng_tusr1/vmap4.VCF.v1 --job test_plink --mod test_common_only \
  --process_dir /data1/dazheng_tusr1/vmap4.VCF.v1/test_plink/process/main_raw \
  2>&1 | tee run_logs/test_common_only_build.log && \
nextflow run /data/home/tusr1/git/script/workflow/Genetics/subworkflows/local/entry/partial_router.nf \
  -c /data/home/tusr1/git/script/workflow/Genetics/nextflow.config \
  --home_dir /data/home/tusr1/01projects/vmap4 --user_dir /data/home/tusr1 --src_dir /data/home/tusr1/git/script/src \
  --output_dir /data1/dazheng_tusr1/vmap4.VCF.v1 --job test_plink --partial_task thin_common_miss_compare \
  --compare_process_dir /data1/dazheng_tusr1/vmap4.VCF.v1/test_plink/process/test_common_only \
  --output_prefix test_thin_vs_test_common_only --left_mod_label test_thin --right_mod_label test_common_only \
  2>&1 | tee run_logs/test_thin_vs_test_common_only_miss_slope.log'
```
<!-- /nf-replay -->

## 2026-07-05 — popdep QC scatter trim99 + CV y-axis floor

- `_plot_popdep_depth_scatter` (`popdep.py`): for both `depth` and `reldepth`, each mean-vs-dispersion scatter also writes a `.trim99.png` variant excluding the outer 1% on each axis; raw CV-on-y plots use `y_lim` bottom 0.
- `plot_regression_comparison` (`graph.py`): partial `y_lim` tuples (`(0, None)`, `(None, 0)`) apply via `bottom`/`top` kwargs.
- Smoke: trim helper + `_scatter_y_lim` — **pass** (no pytest file added).

## 2026-07-05 — popdep QC trim99 replot (test_thin + test_common_thin)

- Run folder: `10stats.genome/43run_popdep_qc_trim99/`; screen `popdep_trim99`; both mods exit 0 (8 succeeded each).
- Publish: **64** `*.trim99.png` under `test_plink/stats/{test_thin,test_common_thin}/plots/`.

<!-- nf-replay -->
#### NF replay — popdep_stats trim99 replot

Working directory: `/data/home/tusr1/01projects/vmap4/10stats.genome/43run_popdep_qc_trim99`. Outcome: exit 0, 8+8 succeeded (~9m per mod).

```bash
source ~/.bashrc && conda activate run && \
screen -dmS popdep_trim99 bash -lc 'source ~/.bashrc && conda activate run && cd /data/home/tusr1/01projects/vmap4/10stats.genome/43run_popdep_qc_trim99 && \
for mod in test_thin test_common_thin; do \
  nextflow run /data/home/tusr1/git/script/workflow/Genetics/subworkflows/tmp/popdep_stats.nf \
    -c /data/home/tusr1/git/script/workflow/Genetics/nextflow.config \
    --home_dir /data/home/tusr1/01projects/vmap4 --user_dir /data/home/tusr1 --src_dir /data/home/tusr1/git/script/src \
    --output_dir /data1/dazheng_tusr1/vmap4.VCF.v1 --job test_plink --mod "${mod}" \
    2>&1 | tee -a "run_logs/popdep_qc_trim99_${mod}.log"; \
done'
```
<!-- /nf-replay -->
