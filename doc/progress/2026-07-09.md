## 2026-07-09 — MAF syntenic sites main analysis (k=2)

- Added `genetics.genomics.alignment.maf_syntenic`: streaming MAF parser, cross-chr filter for chr-named queries, k-of-n column extraction (default k=2), wide matrix output (`*.matrix.tsv.gz`).
- Added `subworkflows/tmp/maf_syntenic_sites.nf`; unit tests `tests/test_maf_syntenic.py` pass.
- Launched all 21 chromosomes in screen `mafsyntenic` (`03run_maf_syntenic_sites`); publish to `00data/06maf/syntenic_sites_k2/`.

<!-- nf-replay -->
#### NF replay — maf_syntenic_sites all chr k=2

Working directory: `/data/home/tusr1/01projects/vmap4/10stats.genome/03run_maf_syntenic_sites`. Outcome: running.

```bash
source ~/.bashrc && conda activate run && \
screen -dmS mafsyntenic bash -lc 'source ~/.bashrc && conda activate run && cd /data/home/tusr1/01projects/vmap4/10stats.genome/03run_maf_syntenic_sites && nextflow run /data/home/tusr1/git/script/workflow/Genetics/subworkflows/tmp/maf_syntenic_sites.nf -c /data/home/tusr1/git/script/workflow/Genetics/nextflow.config --user_dir /data/home/tusr1 --home_dir /data/home/tusr1/01projects/vmap4 --maf_dir /data/home/tusr1/01projects/vmap4/00data/06maf/subset_wheat_aegilops --species_list /data/home/tusr1/01projects/vmap4/00data/06maf/species_wheat_aegilops_synteny.txt --chromosomes 1A,1B,1D,2A,2B,2D,3A,3B,3D,4A,4B,4D,5A,5B,5D,6A,6B,6D,7A,7B,7D --min_species 2 --publish_dir /data/home/tusr1/01projects/vmap4/00data/06maf/syntenic_sites_k2 2>&1 | tee run_logs/nextflow.log'
```
<!-- /nf-replay -->

## 2026-07-09 — MAF wheat–Aegilops complex subset (16 species, all chr)

- Redesigned `species_wheat_aegilops_synteny.txt` per user-defined A/B/D complex: 6+4+6 MAF prefixes (`trdur*` = *T. turgidum*); dropped `aesha`, `hovul`.
- Launched full 21-chromosome `maf_species_subset` in screen `mafsubset` (`02run_maf_species_subset_allchr`); publishes to `00data/06maf/subset_wheat_aegilops/`.

<!-- nf-replay -->
#### NF replay — maf_species_subset all chr

Working directory: `/data/home/tusr1/01projects/vmap4/10stats.genome/02run_maf_species_subset_allchr`. Outcome: running.

```bash
source ~/.bashrc && conda activate run && \
screen -dmS mafsubset bash -lc 'source ~/.bashrc && conda activate run && cd /data/home/tusr1/01projects/vmap4/10stats.genome/02run_maf_species_subset_allchr && nextflow run /data/home/tusr1/git/script/workflow/Genetics/subworkflows/tmp/maf_species_subset.nf -c /data/home/tusr1/git/script/workflow/Genetics/nextflow.config --user_dir /data/home/tusr1 --home_dir /data/home/tusr1/01projects/vmap4 --maf_dir /data/home/tusr1/01projects/vmap4/00data/06maf --species_list /data/home/tusr1/01projects/vmap4/00data/06maf/species_wheat_aegilops_synteny.txt --chromosomes 1A,1B,1D,2A,2B,2D,3A,3B,3D,4A,4B,4D,5A,5B,5D,6A,6B,6D,7A,7B,7D --publish_dir /data/home/tusr1/01projects/vmap4/00data/06maf/subset_wheat_aegilops 2>&1 | tee run_logs/nextflow.log'
```
<!-- /nf-replay -->

## 2026-07-09 — MAF triticeae species inventory + mafSpeciesSubset tmp workflow

- Inventoried 29 species in `00data/06maf/*_MZ_29_triticeae.maf.gz`; wheat reference prefix per subgenome is `traesA` / `traesB` / `traesD` on A/B/D chromosomes respectively; other 28 species shared across chromosomes.
- Installed `ucsc-mafspeciessubset` and `ucsc-mafspecieslist` in conda `stats`; pinned in `environment_stats.yml`.
- Added `species_wheat_aegilops_synteny.txt` (16-species wheat–Aegilops collinearity starter set) and `subworkflows/tmp/maf_species_subset.nf`.
- Smoke run `10stats.genome/01run_maf_species_subset` on 1A: subset MAF retains 16 species (29→16); fixed post-gzip grep bug in process script.

<!-- nf-replay -->
#### NF replay — maf_species_subset 1A smoke

Working directory: `/data/home/tusr1/01projects/vmap4/10stats.genome/01run_maf_species_subset`. Outcome: exit 0 after script fix (1A → 16 species).

```bash
source ~/.bashrc && conda activate run && \
cd /data/home/tusr1/01projects/vmap4/10stats.genome/01run_maf_species_subset && \
nextflow run /data/home/tusr1/git/script/workflow/Genetics/subworkflows/tmp/maf_species_subset.nf \
  -c /data/home/tusr1/git/script/workflow/Genetics/nextflow.config \
  --user_dir /data/home/tusr1 --home_dir /data/home/tusr1/01projects/vmap4 \
  --maf_dir /data/home/tusr1/01projects/vmap4/00data/06maf \
  --species_list /data/home/tusr1/01projects/vmap4/00data/06maf/species_wheat_aegilops_synteny.txt \
  --chromosomes 1A \
  --publish_dir /data/home/tusr1/01projects/vmap4/00data/06maf/subset_wheat_aegilops
```
<!-- /nf-replay -->


- Confirmed two duplicate screen jobs: `grpmaf` (`117run_group_maf_inter`, offline `run_all_chr_group_maf.py`) and `tcommon_inter` (`121run_test_common_inter`, NF `build_chr_rare_inter_extract`) — both computing per-chr group MAF intersection on main_raw.
- Stopped both screens and killed orphan 121run Nextflow/python workers.
- Parallel wiring: `_run_group_freq_panel` uses `ProcessPoolExecutor` for per-group `plink2 --freq` (`group_workers`, `plink_threads`); NF `build_chr_rare_inter_extract` label `rare_inter_extract` (16 cpus, maxForks 8); params `rare_inter_group_workers=6`, `rare_inter_plink_threads=4`.
- Relaunched single pipeline run in `122run_test_common_inter` (screen `tcommon_inter`); 8 chr-level extract tasks at launch.

<!-- nf-replay -->
#### NF replay — test_common_inter restart (parallel extract)

Working directory: `/data/home/tusr1/01projects/vmap4/08stats.genome/122run_test_common_inter`. Outcome: running (8/45 rare-inter extract at log time).

```bash
source ~/.bashrc && conda activate run && \
screen -dmS tcommon_inter bash -lc 'source ~/.bashrc && conda activate run && cd /data/home/tusr1/01projects/vmap4/08stats.genome/122run_test_common_inter && nextflow run /data/home/tusr1/git/script/workflow/Genetics/main.nf -c /data/home/tusr1/git/script/workflow/Genetics/nextflow.config --home_dir /data/home/tusr1/01projects/vmap4 --user_dir /data/home/tusr1 --src_dir /data/home/tusr1/git/script/src --output_dir /data1/dazheng_tusr1/vmap4.VCF.v1 --job test_plink --mod test_common_inter --process_dir /data1/dazheng_tusr1/vmap4.VCF.v1/test_plink/process/main_raw 2>&1 | tee run_logs/nextflow.log'
```
<!-- /nf-replay -->

## 2026-07-09 — Empty rare-inter extract skip (chr000/chr043 thin)

- Diagnosed thin pfile exit 13: plink2 `--extract` on empty `rare_inter.txt` (not a crash). chr000/chr043 have 9 sample_groups and **0** all-group-rare sites (confirmed in `117run` chr000 summary too).
- Fix: filter empty extracts before `subsampling_common_inter_pfile_for_test`; defensive skip in process script. Resumed `48run_test_common_inter` with `-resume`.


- Stopped failed `122run` (08stats): 17/45 extract tasks OOM/segfault under maxForks=8 × group_workers=6.
- Memory fix: plink2 workers return paths only; `rare_inter_mask_from_freq_paths` loads one group afreq at a time; fixed empty `all_groups_rare` KeyError (chr043).
- Conservative params: `maxForks=3`, `group_workers=2`, `plink_threads=8`, `memory=256.GB`, `errorStrategy=retry` (maxRetries=2) on `rare_inter_extract`.
- Agent policy: strengthened workstation-core guardrail 9 — prefer `10stats.genome`, treat `08stats.genome` as historical; when `NN>=99` create successor module (+2 prefix, e.g. `12stats.genome`).
- Relaunched in `10stats.genome/48run_test_common_inter` (screen `tcommon_inter`).

<!-- nf-replay -->
#### NF replay — test_common_inter conservative rerun

Working directory: `/data/home/tusr1/01projects/vmap4/10stats.genome/48run_test_common_inter`. Outcome: running (maxForks=3, group_workers=2).

```bash
source ~/.bashrc && conda activate run && \
screen -dmS tcommon_inter bash -lc 'source ~/.bashrc && conda activate run && cd /data/home/tusr1/01projects/vmap4/10stats.genome/48run_test_common_inter && nextflow run /data/home/tusr1/git/script/workflow/Genetics/main.nf -c /data/home/tusr1/git/script/workflow/Genetics/nextflow.config --home_dir /data/home/tusr1/01projects/vmap4 --user_dir /data/home/tusr1 --src_dir /data/home/tusr1/git/script/src --output_dir /data1/dazheng_tusr1/vmap4.VCF.v1 --job test_plink --mod test_common_inter --process_dir /data1/dazheng_tusr1/vmap4.VCF.v1/test_plink/process/main_raw 2>&1 | tee run_logs/nextflow.log'
```
<!-- /nf-replay -->
 per-chr all-sample_group rare-inter extract (`build_chr_rare_inter_extract`, global minor allele aligned MAF < `hf_maf`) then `plink2 --extract --thin 0.0014` (`hf_common_inter_thin_rate`); wired `RUN_TEST_COMMON_INTER` in `main.nf`.
- `test_plink_stats` now runs IBS square + PLINK2 PCA + MDS/plots on merged subgenome pfiles for every test mod.
- `plot_population_structure` publishes PCA variance as `*.sample.pca.variance.png` (full PC bar chart; dropped redundant PC6-only plot).
- Launched `121run_test_common_inter` (screen `tcommon_inter`, `--process_dir` main_raw); `118–120run_pca_pc6_replot_*` for existing mods — all 3 exit 0 (4 plot tasks each).

<!-- nf-replay -->
#### NF replay — test_common_inter full pipeline

Working directory: `/data/home/tusr1/01projects/vmap4/08stats.genome/121run_test_common_inter`. Outcome: running (rare-inter extract 0/45 at log time).

```bash
source ~/.bashrc && conda activate run && \
screen -dmS tcommon_inter bash -lc 'source ~/.bashrc && conda activate run && cd /data/home/tusr1/01projects/vmap4/08stats.genome/121run_test_common_inter && nextflow run /data/home/tusr1/git/script/workflow/Genetics/main.nf -c /data/home/tusr1/git/script/workflow/Genetics/nextflow.config --home_dir /data/home/tusr1/01projects/vmap4 --user_dir /data/home/tusr1 --src_dir /data/home/tusr1/git/script/src --output_dir /data1/dazheng_tusr1/vmap4.VCF.v1 --job test_plink --mod test_common_inter --process_dir /data1/dazheng_tusr1/vmap4.VCF.v1/test_plink/process/main_raw 2>&1 | tee run_logs/nextflow.log'
```
<!-- /nf-replay -->

<!-- nf-replay -->
#### NF replay — PCA PC6 variance replot (3 mods)

Working directory: `08stats.genome/118–120run_pca_pc6_replot_*`. Outcome: exit 0, 4 succeeded each.

```bash
source ~/.bashrc && conda activate run && \
for mod in test_thin test_common_thin test_common_only; do \
  run=118run_pca_pc6_replot_test_thin; \
  [ "$mod" = test_common_thin ] && run=119run_pca_pc6_replot_test_common_thin; \
  [ "$mod" = test_common_only ] && run=120run_pca_pc6_replot_test_common_only; \
  screen -dmS pc6_${mod#test_} bash -lc "source ~/.bashrc && conda activate run && cd /data/home/tusr1/01projects/vmap4/08stats.genome/${run} && nextflow run /data/home/tusr1/git/script/workflow/Genetics/subworkflows/tmp/pca_plot_replot.nf -c /data/home/tusr1/git/script/workflow/Genetics/nextflow.config --home_dir /data/home/tusr1/01projects/vmap4 --user_dir /data/home/tusr1 --src_dir /data/home/tusr1/git/script/src --output_dir /data1/dazheng_tusr1/vmap4.VCF.v1 --job test_plink --mod ${mod} 2>&1 | tee run_logs/nextflow.log"; \
done
```
<!-- /nf-replay -->
