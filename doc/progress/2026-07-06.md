# Progress — 2026-07-06

## 2026-07-06 — Variant MAF/miss plots with sample_group coloring

- Added `src/python/genetics/genomics/variant/maf_miss_group.py`: per-group plink2 `--missing`, tag each variant with dominant missing `Group`, stacked MAF/miss histograms + joint MAF vs F_MISS regression (MAF 0–0.01, miss 0–1).
- Wired `variant_maf_miss_group_stats` into `test_plink_stats` (`stats.nf` / `stats_variant.nf`); tmp entry `subworkflows/tmp/variant_maf_miss_group_replot.nf`.
- Replot `test_thin`: run `08stats.genome/102run_variant_maf_miss_group_replot`, exit 0, 4 succeeded (~2m); **16** PNGs under `test_plink/stats/test_thin/plots/*.group.png`.

<!-- nf-replay -->
#### NF replay — variant_maf_miss_group_replot test_thin

Working directory: `/data/home/tusr1/01projects/vmap4/08stats.genome/102run_variant_maf_miss_group_replot`. Outcome: exit 0, 4 succeeded.

```bash
source ~/.bashrc && conda activate run && \
screen -dmS var_maf_miss_grp bash -lc 'source ~/.bashrc && conda activate run && cd /data/home/tusr1/01projects/vmap4/08stats.genome/102run_variant_maf_miss_group_replot && \
nextflow run /data/home/tusr1/git/script/workflow/Genetics/subworkflows/tmp/variant_maf_miss_group_replot.nf \
  -c /data/home/tusr1/git/script/workflow/Genetics/nextflow.config \
  --home_dir /data/home/tusr1/01projects/vmap4 --user_dir /data/home/tusr1 --src_dir /data/home/tusr1/git/script/src \
  --output_dir /data1/dazheng_tusr1/vmap4.VCF.v1 --job test_plink --mod test_thin \
  2>&1 | tee run_logs/variant_maf_miss_group_replot.log'
```
<!-- /nf-replay -->

## 2026-07-06 — MAF-bin dominant-group stacked proportion bar

- Added `plot_stacked_proportion_bar` (`graph.py`) and `_maf_bin_group_fraction_table` (`maf_miss_group.py`): four MAF bins (0–0.01, 0.01–0.05, 0.05–0.1, 0.1–0.5), 100% stacked bar of dominant missing `Group` fraction per bin; info TSV `*.maf_bin.group_fraction.info.tsv`.
- Replot `test_thin`: `103run_variant_maf_miss_group_replot`, exit 0, 4 succeeded; PNG `*.maf_bin.group_fraction.bar.png` per subgenome.

## 2026-07-06 — D sample_group MAF vs miss regression (replace dist plots)

- Replaced D-only miss histograms with MAF (cohort) vs F_MISS (D samples) scatter+OLS/Huber regressions for MAF 0–0.01 / 0–0.05 / 0–0.5; removed `*.group_D.miss.dist.maf*` publish artefacts.
- Replot `test_thin`: `105run_variant_maf_miss_D_reg_replot`, exit 0; `*.group_D.reg.maf{0-0.01,0-0.05,0-0.5}.png` per subgenome.

## 2026-07-06 — test_rare_only mod (max-MAF rare + 0.1% thin)

- New mod **`test_rare_only`**: per-chr `plink2 --max-maf 0.05 --thin 0.001` (no `--geno`); keeps rare variants (MAF ≤ 0.05) with test_thin-scale subsampling.
- Code: `subsampling_rare_only_pfile_for_test`, `test_rare_only_processor`, `RUN_TEST_RARE_ONLY` in `main.nf`.
- Run `106run_test_rare_only_build/` (screen `test_rare_only`): full build + ref IBS stats, then `thin_common_miss_compare` **test_common_only → test_rare_only** slope plots.

<!-- nf-replay -->
#### NF replay — test_rare_only build + miss slope vs test_common_only

Working directory: `/data/home/tusr1/01projects/vmap4/08stats.genome/106run_test_rare_only_build`.

```bash
source ~/.bashrc && conda activate run && \
screen -dmS test_rare_only bash -lc 'source ~/.bashrc && conda activate run && cd /data/home/tusr1/01projects/vmap4/08stats.genome/106run_test_rare_only_build && \
nextflow run /data/home/tusr1/git/script/workflow/Genetics/main.nf \
  -c /data/home/tusr1/git/script/workflow/Genetics/nextflow.config \
  --home_dir /data/home/tusr1/01projects/vmap4 --user_dir /data/home/tusr1 --src_dir /data/home/tusr1/git/script/src \
  --output_dir /data1/dazheng_tusr1/vmap4.VCF.v1 --job test_plink --mod test_rare_only \
  --process_dir /data1/dazheng_tusr1/vmap4.VCF.v1/test_plink/process/main_raw \
  2>&1 | tee run_logs/test_rare_only_build.log && \
nextflow run /data/home/tusr1/git/script/workflow/Genetics/subworkflows/local/entry/partial_router.nf \
  -c /data/home/tusr1/git/script/workflow/Genetics/nextflow.config \
  --home_dir /data/home/tusr1/01projects/vmap4 --user_dir /data/home/tusr1 --src_dir /data/home/tusr1/git/script/src \
  --output_dir /data1/dazheng_tusr1/vmap4.VCF.v1 --job test_plink --partial_task thin_common_miss_compare \
  --thin_process_dir /data1/dazheng_tusr1/vmap4.VCF.v1/test_plink/process/test_common_only \
  --compare_process_dir /data1/dazheng_tusr1/vmap4.VCF.v1/test_plink/process/test_rare_only \
  --output_prefix test_common_only_vs_test_rare_only --left_mod_label test_common_only --right_mod_label test_rare_only \
  2>&1 | tee run_logs/test_common_only_vs_test_rare_only_miss_slope.log'
```
<!-- /nf-replay -->

## 2026-07-06 — IBS-matrix PCA (PLINK1, not KING)

- Constraint: no plink2 `--make-king`; use **PLINK1.9 `--distance ibs square`** on merged `{subgenome}_test.plink` bfiles.
- Added `plot_ibs_matrix_pca` / `_classical_mds` (`ibs.py`); fixed `load_ids_file` for headerless `.mibs.id`.
- NF: `plink1_ibs_square`, `sample_ibs_pca_plot` (`stats_sample.nf`); tmp `subworkflows/tmp/ibs_pca_replot.nf` for `test_thin` / `test_common_thin` / `test_common_only`.
- Publish: matrices → `process/{mod}/ibs/`; plots → `stats/{mod}/plots/{A,B,D,Others}.sample.ibs.ibs_pca*.png`.
- Tests `tests/test_ibs_matrix_pca.py`: pass.
- Launched `test_thin` in screen `ibs_pca_thin` (`107run_ibs_pca_replot_test_thin`); ~7700 samples × 4 subgenomes — long runtime expected.

<!-- nf-replay -->
#### NF replay — ibs_pca_replot test_thin

Working directory: `/data/home/tusr1/01projects/vmap4/08stats.genome/107run_ibs_pca_replot_test_thin`. Outcome: pending (screen `ibs_pca_thin`).

```bash
source ~/.bashrc && conda activate run && \
screen -dmS ibs_pca_thin bash -lc 'source ~/.bashrc && conda activate run && cd /data/home/tusr1/01projects/vmap4/08stats.genome/107run_ibs_pca_replot_test_thin && \
nextflow run /data/home/tusr1/git/script/workflow/Genetics/subworkflows/tmp/ibs_pca_replot.nf \
  -c /data/home/tusr1/git/script/workflow/Genetics/nextflow.config \
  --home_dir /data/home/tusr1/01projects/vmap4 --user_dir /data/home/tusr1 --src_dir /data/home/tusr1/git/script/src \
  --output_dir /data1/dazheng_tusr1/vmap4.VCF.v1 --job test_plink --mod test_thin \
  2>&1 | tee run_logs/ibs_pca_replot.log'
```
<!-- /nf-replay -->

## 2026-07-06 — popdep scatter adaptive upper-axis trim

- Replaced hard 1% percentile row filter (`_trim_scatter_percentile_outliers`) with gap-based adaptive max trim in `graph.py` (`adaptive_axis_max`, `axis_limits_with_adaptive_upper`): peel isolated high outliers from the top; keep dense multi-point tails; minimum unchanged.
- `_plot_popdep_depth_scatter` (`popdep.py`): trim variant uses axis limits on full data (`.trim.png` replaces `.trim99.png`); CV y-axis floor 0 preserved.
- Tests: `tests/test_adaptive_axis_trim.py` — 7 passed.
- Replot: `10stats.genome/44run_popdep_qc_trim_replot`; screen `popdep_trim_replot`; both mods exit 0 (8 succeeded each). Publish: **24** + **27** `*.trim.png` under `test_plink/stats/{test_thin,test_common_thin}/plots/` (only when trim applies).

<!-- nf-replay -->
#### NF replay — popdep adaptive trim replot

Working directory: `/data/home/tusr1/01projects/vmap4/10stats.genome/44run_popdep_qc_trim_replot`. Outcome: exit 0, 8+8 succeeded (~23m + ~15m).

```bash
source ~/.bashrc && conda activate run && \
screen -dmS popdep_trim_replot bash -lc 'source ~/.bashrc && conda activate run && cd /data/home/tusr1/01projects/vmap4/10stats.genome/44run_popdep_qc_trim_replot && \
for mod in test_thin test_common_thin; do \
  nextflow run /data/home/tusr1/git/script/workflow/Genetics/subworkflows/tmp/popdep_stats.nf \
    -c /data/home/tusr1/git/script/workflow/Genetics/nextflow.config \
    --home_dir /data/home/tusr1/01projects/vmap4 --user_dir /data/home/tusr1 --src_dir /data/home/tusr1/git/script/src \
    --output_dir /data1/dazheng_tusr1/vmap4.VCF.v1 --job test_plink --mod "${mod}" \
    2>&1 | tee -a "run_logs/popdep_qc_trim_replot_${mod}.log"; \
done'
```
<!-- /nf-replay -->

## 2026-07-06 — IBS MDS + PLINK1 PCA replot (3 mods)

- Split IBS ordination (`plot_ibs_matrix_mds`, `MDS1/2…`, `*.ibs_mds.*`) from PLINK1 genotype PCA (`plink1_pca` + `plot_population_structure`, `*.sample.pca.*`); extended `load_plink_eigenvec` for headerless plink1 `.eigenvec`.
- Launched updated `ibs_pca_replot.nf` in screen for `test_thin`, `test_common_thin`, `test_common_only` (prior `107run` interrupted on old workflow).

<!-- nf-replay -->
#### NF replay — ibs_pca_replot three mods (108–110)

Working directories: `08stats.genome/108run_ibs_pca_replot_test_thin`, `109run_…_test_common_thin`, `110run_…_test_common_only`. Outcome: pending.

```bash
source ~/.bashrc && conda activate run && \
screen -dmS ibs_pca_thin bash -lc 'source ~/.bashrc && conda activate run && cd /data/home/tusr1/01projects/vmap4/08stats.genome/108run_ibs_pca_replot_test_thin && \
nextflow run /data/home/tusr1/git/script/workflow/Genetics/subworkflows/tmp/ibs_pca_replot.nf \
  -c /data/home/tusr1/git/script/workflow/Genetics/nextflow.config \
  --home_dir /data/home/tusr1/01projects/vmap4 --user_dir /data/home/tusr1 --src_dir /data/home/tusr1/git/script/src \
  --output_dir /data1/dazheng_tusr1/vmap4.VCF.v1 --job test_plink --mod test_thin \
  2>&1 | tee run_logs/ibs_pca_replot.log'

source ~/.bashrc && conda activate run && \
screen -dmS ibs_pca_common_thin bash -lc 'source ~/.bashrc && conda activate run && cd /data/home/tusr1/01projects/vmap4/08stats.genome/109run_ibs_pca_replot_test_common_thin && \
nextflow run /data/home/tusr1/git/script/workflow/Genetics/subworkflows/tmp/ibs_pca_replot.nf \
  -c /data/home/tusr1/git/script/workflow/Genetics/nextflow.config \
  --home_dir /data/home/tusr1/01projects/vmap4 --user_dir /data/home/tusr1 --src_dir /data/home/tusr1/git/script/src \
  --output_dir /data1/dazheng_tusr1/vmap4.VCF.v1 --job test_plink --mod test_common_thin \
  2>&1 | tee run_logs/ibs_pca_replot.log'

source ~/.bashrc && conda activate run && \
screen -dmS ibs_pca_common_only bash -lc 'source ~/.bashrc && conda activate run && cd /data/home/tusr1/01projects/vmap4/08stats.genome/110run_ibs_pca_replot_test_common_only && \
nextflow run /data/home/tusr1/git/script/workflow/Genetics/subworkflows/tmp/ibs_pca_replot.nf \
  -c /data/home/tusr1/git/script/workflow/Genetics/nextflow.config \
  --home_dir /data/home/tusr1/01projects/vmap4 --user_dir /data/home/tusr1 --src_dir /data/home/tusr1/git/script/src \
  --output_dir /data1/dazheng_tusr1/vmap4.VCF.v1 --job test_plink --mod test_common_only \
  2>&1 | tee run_logs/ibs_pca_replot.log'
```
<!-- /nf-replay -->
