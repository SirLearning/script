# Progress — 2026-07-10

## 2026-07-10 — MAF syntenic matrix fixed 16-species columns + rerun

- Fixed `maf_syntenic.py`: matrix header now uses full `species_wheat_aegilops_synteny.txt` upfront (16 columns, ref species first) instead of freezing on first MAF block.
- Updated `tests/test_maf_syntenic.py`; pytest pass.
- Relaunched all 21 chr in screen `mafsyntenic2` (`05run_maf_syntenic_sites`); overwrites `00data/06maf/syntenic_sites_k2/`.

<!-- nf-replay -->
#### NF replay — maf_syntenic_sites fixed columns rerun

Working directory: `/data/home/tusr1/01projects/vmap4/10stats.genome/05run_maf_syntenic_sites`. Outcome: running.

```bash
source ~/.bashrc && conda activate run && \
screen -dmS mafsyntenic2 bash -lc 'source ~/.bashrc && conda activate run && cd /data/home/tusr1/01projects/vmap4/10stats.genome/05run_maf_syntenic_sites && nextflow run /data/home/tusr1/git/script/workflow/Genetics/subworkflows/tmp/maf_syntenic_sites.nf -c /data/home/tusr1/git/script/workflow/Genetics/nextflow.config --user_dir /data/home/tusr1 --home_dir /data/home/tusr1/01projects/vmap4 --maf_dir /data/home/tusr1/01projects/vmap4/00data/06maf/subset_wheat_aegilops --species_list /data/home/tusr1/01projects/vmap4/00data/06maf/species_wheat_aegilops_synteny.txt --chromosomes 1A,1B,1D,2A,2B,2D,3A,3B,3D,4A,4B,4D,5A,5B,5D,6A,6B,6D,7A,7B,7D --min_species 2 --publish_dir /data/home/tusr1/01projects/vmap4/00data/06maf/syntenic_sites_k2 2>&1 | tee run_logs/nextflow.log'
```
<!-- /nf-replay -->

## 2026-07-10 — test_common_inter afreq-light resume (48run)

- Interrupted `tcommon_inter` (exit 129 on chr007/chr008/chr019); implemented `_load_afreq_light` (ID + ALT_FREQS only, no Position mapping), published-afreq reuse, and skip when published `rare_inter.txt` + summary exist.
- `build_chr_rare_inter_extract`: publish `extract/afreq/{chr_id}/{chr_id}.{global,GROUP}.afreq`; NF passes `published_afreq_dir` / skip paths.
- One-off migration from `48run/work/*/plink_freq_scratch/` → publish `extract/afreq/` (**34** chr); backfilled **27** missing `extract/info/*.rare_inter.summary.tsv` from work.
- Fixed `_load_afreq_light` header parse (`#CHROM` line, not `comment='#'`).
- Resumed `10stats.genome/48run_test_common_inter` with `-resume` in screen `tcommon_inter`; completed extract chr skip via publish tree; pending chr reuse published afreq + light mask.

<!-- nf-replay -->
#### NF replay — test_common_inter afreq-light resume

Working directory: `/data/home/tusr1/01projects/vmap4/10stats.genome/48run_test_common_inter`. Outcome: resumed after interrupt.

```bash
source ~/.bashrc && conda activate run && \
screen -dmS tcommon_inter bash -lc 'source ~/.bashrc && conda activate run && cd /data/home/tusr1/01projects/vmap4/10stats.genome/48run_test_common_inter && nextflow run /data/home/tusr1/git/script/workflow/Genetics/main.nf -c /data/home/tusr1/git/script/workflow/Genetics/nextflow.config --home_dir /data/home/tusr1/01projects/vmap4 --user_dir /data/home/tusr1 --src_dir /data/home/tusr1/git/script/src --output_dir /data1/dazheng_tusr1/vmap4.VCF.v1 --job test_plink --mod test_common_inter --process_dir /data1/dazheng_tusr1/vmap4.VCF.v1/test_plink/process/main_raw -resume 2>&1 | tee -a run_logs/nextflow.log'
```
<!-- /nf-replay -->
